# 运行期依赖 —— 刻意保持最小面（与 jimeng / pavo 同源约定）。
# HTTP 栈只留 httpx：少一类超时语义 / 连接池 / trust_env 的行为差异。
fastapi>=0.115
uvicorn>=0.30
gunicorn>=22.0
# gunicorn 的 ASGI worker 必须用这个独立包：uvicorn 自带的 workers 已弃用。
uvicorn-worker>=0.3
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.2
# 日志（与兄弟服务同源）；可观测性 SDK 在 requirements-otel.txt（可选）
loguru>=0.7
